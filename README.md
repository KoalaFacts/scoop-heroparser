# scoop-bucket

Scoop bucket for KoalaFacts tools.

## Installation

To install the `heroparser` CLI utility using Scoop, run:

```powershell
scoop bucket add KoalaFacts https://github.com/KoalaFacts/scoop-bucket.git
scoop install heroparser
```
